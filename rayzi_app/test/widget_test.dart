import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder - app smoke test', () {
    // Full widget tests require Supabase initialization with real
    // credentials. Run integration tests on a device/emulator instead:
    // flutter test integration_test/app_test.dart
    expect(1 + 1, 2);
  });
}
